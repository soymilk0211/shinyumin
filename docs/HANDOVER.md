# 御茗有限公司 品牌電商網站 — 開發交接文件

> 這份文件是一次完整的需求訪談（六輪）之後的結論。所有設計決定都已與業主確認。
> **新的對話請先完整讀完這份文件，不要重新提問已經談定的事項。**

---

## 0. 給接手者的最重要三件事

1. **業主不是工程師。** 全程使用**繁體中文**溝通，解釋要用生活化的比喻，不要預設任何技術背景。
2. **一步一確認。** 每完成一個步驟就**停下來等業主確認**，絕不連續做兩步。禁止一次產出大量檔案 —— 業主無法審閱，會失去對專案的控制感。
3. **每步結束 commit 並 push。** 業主已授權，不需要每次詢問。Remote: `https://github.com/soymilk0211/shinyumin.git`

---

## 1. 專案基本資料

| 項目 | 內容 |
|---|---|
| 專案位置 | `C:\Users\user\Projects\tea-brand-ecommerce` |
| Git remote | `https://github.com/soymilk0211/shinyumin.git` |
| 公司全名 | 御茗有限公司 |
| 英文名 | SHIN-YU-MIN Co., Ltd. |
| 統一編號 | 27464360 |
| 地址 | 南投縣魚池鄉大雁村山楂腳巷 10-20 號 |
| 公開電話 | 049-2896602 |
| 公開 Email | k6123@ms22.hinet.net |
| 品牌定位 | 日月潭魚池產區紅茶，家族製茶 |

**隱私規則（重要）**：業主母親的個人手機號碼**刻意不寫入本文件、不放上網站**。公司市話即為住家電話，業主已知並同意公開。網站上的 email 需做防爬蟲處理。

**品牌故事的核心事實**：公司地址就位在南投縣魚池鄉大雁村 —— 日月潭紅茶的核心產區。品牌故事應以「我們就在魚池」為主軸，寫實際的產區與家族工藝，不要寫空泛的形容詞。主力商品是紅玉紅茶（台茶 18 號）。

---

## 2. 詞彙表（Domain Glossary）

第 0 步應將本節寫入專案根目錄的 `CONTEXT.md`。

| 詞 | 定義 |
|---|---|
| **商品 Product** | 一款茶（紅玉、阿薩姆、四季春…）。擁有故事、風味描述、照片、分類。**本身不能被購買。** |
| **規格 Variant** | 一款茶的一種包裝，例如「紅玉 150g 真空罐裝」。**這是唯一能被加入購物車的單位**，有自己的價格與販售狀態。未來的禮盒即為新增規格。 |
| **分類 Category** | 單層分類：紅茶／烏龍／綠茶。**紅茶排第一**（招牌商品）。紅玉與阿薩姆是「商品」不是「分類」。 |
| **購物車 Cart** | 只存在瀏覽器 LocalStorage，僅記錄「規格 ID + 數量」，**絕不儲存價格**。 |
| **訂單 Order** | 一次結帳的完整紀錄。 |
| **訂單明細 Order Item** | 訂單中的一個品項，**儲存下單當下的價格快照**。業主改價不得影響歷史訂單金額。 |
| **顧客 Customer** | 目前僅是訂單上的一組聯絡資料，**不是帳號**。以 email 為鑰匙，未來加入會員系統時可自動歸戶。 |
| **後台 Admin** | 密碼保護的繁體中文手機網頁，業主用來改價、切售罄、看訂單、標記出貨。 |

---

## 3. 技術決定

| 項目 | 決定 | 備註 |
|---|---|---|
| 框架 | Next.js App Router + TypeScript | |
| 樣式 | Tailwind CSS | |
| 資料庫 | **Supabase (PostgreSQL)** | 免費方案：500MB、**API 請求無上限**、閒置一週會暫停 |
| 後台 | 自建繁中手機網頁 | 業主明確選擇，理由見第 6 節 |
| 後台驗證 | **單一密碼**存於環境變數 | 未來可升級為 Supabase Auth，不需重寫 |
| 購物車 | Zustand + LocalStorage persist | 注意處理 hydration mismatch |
| i18n | zh / en 網址路由 | 介面文字用翻譯檔；**商品內容存資料庫雙欄** |
| 寄信 | Resend | **必須有自有網域才能寄信** |
| 即時通知 | LINE Messaging API 推播至內部群組 | |
| 每日報表 | **Claude Cowork 排程** | 取代原規格的 Vercel Cron + LINE |
| 主題 | next-themes 深淺色 | 冷色背景 + **品牌赭紅當重點色** |
| 部署 | Vercel | 開發用免費版，**收錢前必須升 Pro $20/月** |
| AI | **網站端不使用任何 AI API** | 見第 7 節 |

---

## 4. 商業規則

- **幣別**：一律 TWD，價格**含稅**。英文站也顯示 `NT$`。
- **運費**：黑貓 $200 ／ 郵局 $130 ／ 離島（澎金馬）郵局 $250
- **免運**：本島滿 **$2,000** 免運。**離島不適用免運。**
- **運費設定**：寫在程式的設定檔，不放資料庫（不常改，且必須由伺服器計算）
- **付款**：ATM 轉帳，顧客回填轉帳末五碼，人工對帳。程式面將「建立訂單」與「付款」拆開，未來可插入綠界等金流。
- **發票**：網站僅收集統編與抬頭（皆選填），**發票在外部系統人工開立**。
- **訂單編號**：`YM-YYYYMMDD-NNNN`（例：`YM-20260816-0001`）
- **訂單狀態**：待付款 → 已付款 → 已出貨 → 已完成，外加「已取消」
- **庫存**：**不做自動扣庫存**。規格有一個狀態欄位（販售中／售罄／已下架），業主在後台手動切換。
- **改價**：純人工流程。業主政策是調價需公告一個月，但**系統不做排程改價** —— 改為提供一個可開關的公告橫幅。
- **批發**：**第一版不上線**。Footer 寫「大量訂購請電洽」。批發價較低、走大榮貨運、免運門檻 $3,000，但全部由業主私下處理。
- **會員**：**第一版不做**，但訂單需儲存顧客 email 作為未來歸戶的鑰匙（設計上區分「訂單上的顧客資料」與「會員帳號」）。

---

## 5. 資料表設計（Supabase）

```
categories
  id, slug, name_zh, name_en, sort_order

products
  id, slug, category_id (FK),
  name_zh, name_en,
  description_zh, description_en,
  tasting_notes_zh, tasting_notes_en,
  image_filename,           -- 對應 /public/images/products/
  sort_order, is_published, created_at

variants
  id, product_id (FK), sku,
  label_zh,                 -- 例：150g 真空罐裝
  label_en,
  weight_grams,
  price_twd,
  status,                   -- 販售中 / 售罄 / 已下架
  sort_order

orders
  id, order_number,
  customer_name, customer_phone, customer_email,
  shipping_method,          -- 黑貓 / 郵局 / 離島郵局
  shipping_address,
  subtotal_twd, shipping_fee_twd, total_twd,
  payment_method, payment_status, transfer_last5,
  order_status,
  tax_id, invoice_title,    -- 皆可為 null
  note, created_at, updated_at

order_items
  id, order_id (FK),
  variant_id (FK, nullable),
  product_name_zh,          -- 快照
  variant_label_zh,         -- 快照
  unit_price_twd,           -- 快照，絕不從 variants 即時讀取
  quantity, line_total_twd
```

**測試資料**：日月潭魚池台茶 18 號（紅玉紅茶）— 150g 真空罐裝 $650，以及 75g 版本。

---

## 6. 開發步驟

每步結束：commit + push + 在 `docs/` 放一份繁中說明（說明做了什麼、檔案在哪、業主可以怎麼驗證）+ **停下來等確認**。

| 步驟 | 內容 |
|---|---|
| **第 0 步** | 建專案、Next.js 初始化、接上 GitHub remote、撰寫 `CONTEXT.md` |
| **第 1 步** | Supabase：引導業主申請帳號、建立資料表、匯入測試商品 |
| **第 2 步** | UI 基礎：Tailwind、深淺色主題、i18n 路由、Navbar／Footer、圖片佔位符元件 |
| **第 3 步** | 首頁、品牌故事、產品列表、產品詳情（含規格選擇器）、Zustand 購物車 |
| **第 4 步** | 結帳表單、**伺服器端金額重算**、寫入資料庫、顧客確認信 + 店家存檔信 |
| **第 5 步** | LINE 即時訂單通知（含 groupId 擷取引導） |
| **第 6 步** | 手機後台：改價、切售罄、看訂單、標記已付款／已出貨 |
| **第 7 步** | Cowork 每日報表排程（讀昨日訂單） |

**後台第一版只做四件事**：改價、切售罄、看訂單、標記出貨。**不做**新增／刪除商品、不做編輯商品介紹文 —— 那些是偶爾做且需要斟酌用字的事，在電腦上另行處理。

---

## 7. 通知與 AI 的最終架構

**網站端不呼叫任何 AI API。** 這是刻意的決定：

- **即時訂單通知** → LINE 推播，**純模板、不經 AI**。理由：訊息內容是訂單編號與金額，需要的是「快、正確、必定送達」，AI 會帶來延遲、數字寫錯的風險，以及多一個故障點。
- **每日報表** → **Claude Cowork 排程**，使用業主的 Claude 訂閱，不需要任何 API Key。Cowork 直接查詢 Supabase，能做得比一則 LINE 訊息豐富得多，且業主可以追問。

**業主持有長庚大學的 LLM API（`https://air.cgu.edu.tw/cgullmapi/v1`，OpenAI 相容，已驗證可從外網存取）。本專案最終不需要它。** 若未來要用（例如以 `translategemma` 草擬商品英文介紹），請注意：那是學術資源，用於商業網站有政策風險，且綁定學校帳號、畢業即失效。

### 寄信對象

| 信件 | 收件人 | 用途 |
|---|---|---|
| 訂單確認信 | 顧客 | **回覆地址設 `k6123@ms22.hinet.net`** |
| 訂單存檔信 | 業主的 Gmail | 永久紀錄與備援，**不需要有人盯著看** |

**不要**把訂單通知寄到 `k6123@ms22.hinet.net` —— 業主母親只在電腦上收信，且 HiNet 信箱容量有限。**LINE 才是主要通知管道。**

---

## 8. 已知地雷（都已查證，不要重蹈覆轍）

1. **絕不信任前端傳來的價格。** 購物車在 LocalStorage，可被任意竄改。結帳 API 只接受「規格 ID + 數量」，價格、運費、總額**一律由伺服器重新查詢資料庫計算**。這同時也解決了「顧客隔幾天回來結帳、期間業主改過價」的問題。

2. **絕不動業主現有的 LINE 官方帳號。** LINE 的「回應模式」切成聊天機器人以啟用 Webhook 後，**人工聊天功能會失效**。業主的現有帳號是實際在用的客服管道。已另開一個「御茗內部通知」專用帳號，回應模式設為「關閉手動聊天」。第 5 步還需在後台**關閉「自動回應訊息」並啟用「Webhook」**，否則訊息會被 LINE 攔截、不會轉發。

3. **groupId 無法從任何後台複製。** 必須先接一次 webhook，在群組中發言才能從事件中取得。第 5 步需提供互動式引導。

4. **Resend 必須驗證網域才能寄信。** 不能用 `@ms22.hinet.net`（HiNet 的網域，業主無 DNS 控制權）。

5. **Vercel Hobby 方案禁止商業用途。** 開始收錢前必須升級 Pro（$20/月）。另需開啟 Spend Management 設定花費上限並啟用自動暫停，因為超額後是無上限計費。

6. **DDoS 由 Vercel 自動處理，不需自建。** 但**應用層濫用需要自己防** —— 結帳 API 與後台登入必須加上速率限制，避免假訂單灌爆資料庫或密碼被暴力破解。

7. **Cowork 排程只在 app 開啟時執行。** 業主表示電腦不會關機，可接受。

8. **必須啟用 RLS（Row Level Security），這是 Supabase 最常見的重大疏失。** 預設建立的資料表若未啟用 RLS，任何拿到 `anon` 金鑰的人（該金鑰會出現在瀏覽器端，等同公開）就能讀寫全部資料 —— 包含 `orders` 裡的顧客姓名、電話、地址。要求：
   - **所有資料表一律啟用 RLS。**
   - `categories` / `products` / `variants`：只開放 `anon` **讀取**（且僅限 `is_published` / 未下架者），**不得寫入**。
   - `orders` / `order_items`：`anon` **完全不得讀取或寫入**。所有讀寫一律走伺服器端、使用 `service_role` 金鑰。
   - 顧客送出訂單是經由 Next.js 的 API route（伺服器端），**不是**瀏覽器直接寫資料庫。

9. **金鑰處理規則。** 專案網址（`https://<ref>.supabase.co`）不是機密，它會出現在每一個瀏覽器請求裡。但：
   - `anon` 金鑰：可出現在前端，但必須搭配 RLS 才安全。
   - **`service_role` 金鑰：最高權限，繞過所有 RLS。只能存在於伺服器端環境變數，絕不可進入 GitHub、絕不可出現在任何 `NEXT_PUBLIC_*` 變數、絕不可貼進對話。**
   - 確認 `.env.local` 已被 `.gitignore` 涵蓋，並提供 `.env.example` 範本（只有欄位名稱、沒有值）。

8. **Airtable 已評估並否決。** 免費方案僅 **1,000 次 API 呼叫／月**（不是每秒限速，是每月總量），做網站撐不到一天；且介面**沒有繁體中文**（僅英／法／德／西）。這是改用 Supabase 的主因，不要再提議改回去。

---

## 9. 待補項目

| 項目 | 急迫度 | 期限 |
|---|---|---|
| **網域全名與續費狀態** | 🔴 最急 | 第 4 步前。業主表示「有網域但好久沒付錢」，需確認是否仍在名下、是否已被他人搶註（可能影響品牌命名） |
| 商標圖檔 | 🟡 | 第 2 步（先用佔位符） |
| 商品照片 | 🟡 | 第 3 步（先用佔位符）。**圖片放 `/public/images/`，不放資料庫** |
| 完整商品清單 | 🟡 | 第 3 步。已知有綠茶、烏龍、紅茶（紅玉、阿薩姆）；禮盒規劃中 |
| Supabase project | ✅ 已完成 | organization `shinyumin`（Free）、project ref **`eggabnafyigvkwdryytg`**、Compute NANO、狀態 Healthy。**Region 尚未經業主確認** —— 第 1 步開始前請先問清楚；若非 Singapore / Tokyo，且資料庫仍為空，建議直接重建（region 無法變更） |
| Supabase 金鑰 | 🟡 | 第 1 步。需業主自 Settings → API 取得 `anon` 與 `service_role`，寫入 `.env.local`。**取得前不要嘗試連線** |
| Resend 帳號 | 🟢 需引導 | 第 4 步 |
| 業主 Gmail 位址確認 | 🟢 | 第 4 步 |

---

## 10. 視覺方向

- **風格**：現代極簡，藝廊式留白排版
- **色彩**：背景維持清冷極簡（淺色＝薄霧藍系／深色＝午夜紫系），**但所有重點色（按鈕、連結、強調字）使用品牌赭紅**
- **理由**：業主原規格指定紫／藍冷色系，但實際品牌識別（名片、商標）是溫暖的赭紅茶色。純冷色會與實體物料脫節；折衷方案同時保住藝廊感與品牌一致性。
- **實作要求**：所有顏色集中在單一設定檔，方便日後整組更換
- **RWD**：手機與電腦都必須完美適配
- **圖片佔位符**：找不到圖檔時顯示優雅的佔位符，畫面不得崩潰
